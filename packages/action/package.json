{
  "name": "@vulnfuse/action",
  "version": "0.4.4",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node24 --format=cjs --outfile=dist/index.cjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "3.0.1",
    "@actions/glob": "0.7.0",
    "@vulnfuse/core": "0.4.4"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
